<!DOCTYPE html>
<html lang="en">
<link rel="stylesheet" href="/styles/show.css" />

<%- include( 'partials/head.ejs' ) %>

    <body>
        <%- include( 'partials/nav.ejs' ) %>
            <section>
                <div>
                    <h2 class="title">Video: <%=video.name%>
                    </h2>
                </div>
            </section>
            <main class="container">
                <div>
                    <div>
                        <a href="/videos/<%=id%>/edit">Edit Video</a>
                    </div>
                    <div>
                        <iframe width="550" height="400" src="<%=video.url%>">
                        </iframe>
                    </div>
                    <div class="text">
                        <div id="upload">
                            <p>
                                Uploaded by: <%=video.uploader%>
                            </p>
                        </div>
                        <div id="description">
                            <p>
                                <%=video.description%>
                            </p>
                        </div>
                    </div>
                    <div>
                        
                        <form method="POST" action="/comments">
                            <input name="user" type="hidden" value="user1" />
                            <div>
                                <textarea id="commentArea" name="body" rows="7" cols="40" placeholder="Write Comment Here..."></textarea>
                            </div>
                            <select name="video">
                                <option value="<%=video._id%>">
                                    <%=video.title%>
                                </option>
                            </select>
                            <button type="submit">
                                Submit Comment
                            </button>
                        </form>
                    </div>
                    <div class="comments">
                        <h3>Comments:</h3>
                        <% for( let i=0; i<comments.length; i++){ %>
                            <p id="comment">
                                <%=comments[i].body%>   /   <%=comments[i].createdAt%>
                            </p>
                            <%}%>
                        </div>
                    <div>
                        <p class="control">
                        <form action="/videos/<%= id %>?_method=DELETE" method="POST">
                            <input class="button is-danger" type="submit" value="Delete Video" />
                        </form>
                        </p>
                    </div>
                </div>
            </main>
    </body>

</html>